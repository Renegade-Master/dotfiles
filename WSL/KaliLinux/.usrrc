# Add things here to add them to User Environment
alias "update=sudo apt update; sudo apt upgrade -y; sudo apt autoremove -y"
alias "ll=ls -lAg"

# Add CMD to the Path
export "PATH=$PATH:/mnt/c/WINDOWS/system32/cmd.exe"

# Use a local XServer for rendering X11 Applications
export "DISPLAY=:0"

